<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Your Quote</title>
</head>
<body>
  <div class="navbar">
    <!-- Reuse your navbar code from home.handlebars -->
  </div>

  <div class="quote-container">
    <h1>Your Saved Quote:</h1>
    <blockquote>{{latestQuote.text}}</blockquote>
    <p>— {{latestQuote.author}}</p>

    <!-- Optional: Display all quotes -->
    {{#if allQuotes.length}}
      <h2>All Quotes:</h2>
      {{#each allQuotes}}
        <blockquote>{{this.text}}</blockquote>
        <p>— {{this.author}}</p>
      {{/each}}
    {{/if}}
  </div>
</body>
</html>